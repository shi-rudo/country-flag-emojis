import { CountryFlag } from "../types";

/**
 * Represents the country flag for Albania.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Albania flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Albania.
 * @property {string} unicode - The Unicode code points for the Albania flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const AL: CountryFlag = {
  flag: "🇦🇱",
  isoAlpha2: "AL",
  unicode: "U+1F1E6,U+1F1F1",
  nameEnglish: "Albania",
};

export default AL;
