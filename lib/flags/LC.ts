import { CountryFlag } from "../types";

/**
 * Represents the country flag for Saint Lucia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Saint Lucia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Saint Lucia.
 * @property {string} unicode - The Unicode code points for the Saint Lucia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const LC: CountryFlag = {
  flag: "🇱🇨",
  isoAlpha2: "LC",
  unicode: "U+1F1F1,U+1F1E8",
  nameEnglish: "Saint Lucia",
};

export default LC;
