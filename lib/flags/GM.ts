import { CountryFlag } from "../types";

/**
 * Represents the country flag for Gambia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Gambia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Gambia.
 * @property {string} unicode - The Unicode code points for the Gambia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const GM: CountryFlag = {
  flag: "🇬🇲",
  isoAlpha2: "GM",
  unicode: "U+1F1EC,U+1F1F2",
  nameEnglish: "Gambia",
};

export default GM;
