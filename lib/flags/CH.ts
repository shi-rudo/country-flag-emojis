import { CountryFlag } from "../types";

/**
 * Represents the country flag for Switzerland.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Switzerland flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Switzerland.
 * @property {string} unicode - The Unicode code points for the Switzerland flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const CH: CountryFlag = {
  flag: "🇨🇭",
  isoAlpha2: "CH",
  unicode: "U+1F1E8,U+1F1ED",
  nameEnglish: "Switzerland",
};

export default CH;
