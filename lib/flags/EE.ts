import { CountryFlag } from "../types";

/**
 * Represents the country flag for Estonia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Estonia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Estonia.
 * @property {string} unicode - The Unicode code points for the Estonia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const EE: CountryFlag = {
  flag: "🇪🇪",
  isoAlpha2: "EE",
  unicode: "U+1F1EA,U+1F1EA",
  nameEnglish: "Estonia",
};

export default EE;
