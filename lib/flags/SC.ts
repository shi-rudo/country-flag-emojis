import { CountryFlag } from "../types";

/**
 * Represents the country flag for Seychelles.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Seychelles flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Seychelles.
 * @property {string} unicode - The Unicode code points for the Seychelles flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const SC: CountryFlag = {
  flag: "🇸🇨",
  isoAlpha2: "SC",
  unicode: "U+1F1F8,U+1F1E8",
  nameEnglish: "Seychelles",
};

export default SC;
