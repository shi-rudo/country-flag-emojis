import { CountryFlag } from "../types";

/**
 * Represents the country flag for Dominica.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Dominica flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Dominica.
 * @property {string} unicode - The Unicode code points for the Dominica flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const DM: CountryFlag = {
  flag: "🇩🇲",
  isoAlpha2: "DM",
  unicode: "U+1F1E9,U+1F1F2",
  nameEnglish: "Dominica",
};

export default DM;
