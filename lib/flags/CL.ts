import { CountryFlag } from "../types";

/**
 * Represents the country flag for Chile.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Chile flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Chile.
 * @property {string} unicode - The Unicode code points for the Chile flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const CL: CountryFlag = {
  flag: "🇨🇱",
  isoAlpha2: "CL",
  unicode: "U+1F1E8,U+1F1F1",
  nameEnglish: "Chile",
};

export default CL;
