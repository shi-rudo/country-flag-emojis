import { CountryFlag } from "../types";

/**
 * Represents the country flag for Moldova.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Moldova flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Moldova.
 * @property {string} unicode - The Unicode code points for the Moldova flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const MD: CountryFlag = {
  flag: "🇲🇩",
  isoAlpha2: "MD",
  unicode: "U+1F1F2,U+1F1E9",
  nameEnglish: "Moldova",
};

export default MD;
