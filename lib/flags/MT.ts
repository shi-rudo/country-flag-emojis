import { CountryFlag } from "../types";

/**
 * Represents the country flag for Malta.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Malta flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Malta.
 * @property {string} unicode - The Unicode code points for the Malta flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const MT: CountryFlag = {
  flag: "🇲🇹",
  isoAlpha2: "MT",
  unicode: "U+1F1F2,U+1F1F9",
  nameEnglish: "Malta",
};

export default MT;
