import { CountryFlag } from "../types";

/**
 * Represents the country flag for Ethiopia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Ethiopia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Ethiopia.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Ethiopia.
 * @property {string} unicode - The Unicode code points for the Ethiopia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const ET: CountryFlag = {
  flag: "🇪🇹",
  isoAlpha2: "ET",
  isoAlpha3: "ETH",
  unicode: "U+1F1EA,U+1F1F9",
  nameEnglish: "Ethiopia"
};
