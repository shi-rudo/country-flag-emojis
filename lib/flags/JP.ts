import { CountryFlag } from "../types";

/**
 * Represents the country flag for Japan.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Japan flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Japan.
 * @property {string} unicode - The Unicode code points for the Japan flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const JP: CountryFlag = {
  flag: "🇯🇵",
  isoAlpha2: "JP",
  unicode: "U+1F1EF,U+1F1F5",
  nameEnglish: "Japan",
};

export default JP;
