import { CountryFlag } from "../types";

/**
 * Represents the country flag for Somalia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Somalia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Somalia.
 * @property {string} unicode - The Unicode code points for the Somalia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const SO: CountryFlag = {
  flag: "🇸🇴",
  isoAlpha2: "SO",
  unicode: "U+1F1F8,U+1F1F4",
  nameEnglish: "Somalia",
};

export default SO;
