import { CountryFlag } from "../types";

/**
 * Represents the country flag for Netherlands.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Netherlands flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Netherlands.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Netherlands.
 * @property {string} unicode - The Unicode code points for the Netherlands flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const NL: CountryFlag = {
  flag: "🇳🇱",
  isoAlpha2: "NL",
  isoAlpha3: "NLD",
  unicode: "U+1F1F3,U+1F1F1",
  nameEnglish: "Netherlands"
};
