import { CountryFlag } from "../types";

/**
 * Represents the country flag for Uganda.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Uganda flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Uganda.
 * @property {string} unicode - The Unicode code points for the Uganda flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const UG: CountryFlag = {
  flag: "🇺🇬",
  isoAlpha2: "UG",
  unicode: "U+1F1FA,U+1F1EC",
  nameEnglish: "Uganda",
};

export default UG;
