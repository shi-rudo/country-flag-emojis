import { CountryFlag } from "../types";

/**
 * Represents the country flag for Niger.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Niger flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Niger.
 * @property {string} unicode - The Unicode code points for the Niger flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const NE: CountryFlag = {
  flag: "🇳🇪",
  isoAlpha2: "NE",
  unicode: "U+1F1F3,U+1F1EA",
  nameEnglish: "Niger",
};

export default NE;
