import { CountryFlag } from "../types";

/**
 * Represents the country flag for Dominican Republic.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Dominican Republic flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Dominican Republic.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Dominican Republic.
 * @property {string} unicode - The Unicode code points for the Dominican Republic flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const DO: CountryFlag = {
  flag: "🇩🇴",
  isoAlpha2: "DO",
  isoAlpha3: "DOM",
  unicode: "U+1F1E9,U+1F1F4",
  nameEnglish: "Dominican Republic"
};
