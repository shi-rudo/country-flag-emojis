import { CountryFlag } from "../types";

/**
 * Represents the country flag for Syrian Arab Republic.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Syrian Arab Republic flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Syrian Arab Republic.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Syrian Arab Republic.
 * @property {string} unicode - The Unicode code points for the Syrian Arab Republic flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const SY: CountryFlag = {
  flag: "🇸🇾",
  isoAlpha2: "SY",
  isoAlpha3: "SYR",
  unicode: "U+1F1F8,U+1F1FE",
  nameEnglish: "Syrian Arab Republic"
};
