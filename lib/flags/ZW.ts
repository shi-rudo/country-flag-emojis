import { CountryFlag } from "../types";

/**
 * Represents the country flag for Zimbabwe.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Zimbabwe flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Zimbabwe.
 * @property {string} unicode - The Unicode code points for the Zimbabwe flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const ZW: CountryFlag = {
  flag: "🇿🇼",
  isoAlpha2: "ZW",
  unicode: "U+1F1FF,U+1F1FC",
  nameEnglish: "Zimbabwe",
};

export default ZW;
