import { CountryFlag } from "../types";

/**
 * Represents the country flag for South Africa.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the South Africa flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for South Africa.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for South Africa.
 * @property {string} unicode - The Unicode code points for the South Africa flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const ZA: CountryFlag = {
  flag: "🇿🇦",
  isoAlpha2: "ZA",
  isoAlpha3: "ZAF",
  unicode: "U+1F1FF,U+1F1E6",
  nameEnglish: "South Africa"
};
