import { CountryFlag } from "../types";

/**
 * Represents the country flag for Libya.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Libya flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Libya.
 * @property {string} unicode - The Unicode code points for the Libya flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const LY: CountryFlag = {
  flag: "🇱🇾",
  isoAlpha2: "LY",
  unicode: "U+1F1F1,U+1F1FE",
  nameEnglish: "Libya",
};

export default LY;
