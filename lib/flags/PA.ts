import { CountryFlag } from "../types";

/**
 * Represents the country flag for Panama.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Panama flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Panama.
 * @property {string} unicode - The Unicode code points for the Panama flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const PA: CountryFlag = {
  flag: "🇵🇦",
  isoAlpha2: "PA",
  unicode: "U+1F1F5,U+1F1E6",
  nameEnglish: "Panama",
};

export default PA;
