import { CountryFlag } from "../types";

/**
 * Represents the country flag for Ecuador.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Ecuador flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Ecuador.
 * @property {string} unicode - The Unicode code points for the Ecuador flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const EC: CountryFlag = {
  flag: "🇪🇨",
  isoAlpha2: "EC",
  unicode: "U+1F1EA,U+1F1E8",
  nameEnglish: "Ecuador",
};

export default EC;
