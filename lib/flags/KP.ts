import { CountryFlag } from "../types";

/**
 * Represents the country flag for North Korea.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the North Korea flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for North Korea.
 * @property {string} unicode - The Unicode code points for the North Korea flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const KP: CountryFlag = {
  flag: "🇰🇵",
  isoAlpha2: "KP",
  unicode: "U+1F1F0,U+1F1F5",
  nameEnglish: "North Korea",
};

export default KP;
