import { CountryFlag } from "../types";

/**
 * Represents the country flag for Iceland.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Iceland flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Iceland.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Iceland.
 * @property {string} unicode - The Unicode code points for the Iceland flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const IS: CountryFlag = {
  flag: "🇮🇸",
  isoAlpha2: "IS",
  isoAlpha3: "ISL",
  unicode: "U+1F1EE,U+1F1F8",
  nameEnglish: "Iceland"
};
