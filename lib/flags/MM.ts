import { CountryFlag } from "../types";

/**
 * Represents the country flag for Myanmar.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Myanmar flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Myanmar.
 * @property {string} unicode - The Unicode code points for the Myanmar flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const MM: CountryFlag = {
  flag: "🇲🇲",
  isoAlpha2: "MM",
  unicode: "U+1F1F2,U+1F1F2",
  nameEnglish: "Myanmar",
};

export default MM;
