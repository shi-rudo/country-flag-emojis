import { CountryFlag } from "../types";

/**
 * Represents the country flag for Poland.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Poland flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Poland.
 * @property {string} unicode - The Unicode code points for the Poland flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const PL: CountryFlag = {
  flag: "🇵🇱",
  isoAlpha2: "PL",
  unicode: "U+1F1F5,U+1F1F1",
  nameEnglish: "Poland",
};

export default PL;
