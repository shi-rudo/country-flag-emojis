import { CountryFlag } from "../types";

/**
 * Represents the country flag for Costa Rica.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Costa Rica flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Costa Rica.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Costa Rica.
 * @property {string} unicode - The Unicode code points for the Costa Rica flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const CR: CountryFlag = {
  flag: "🇨🇷",
  isoAlpha2: "CR",
  isoAlpha3: "CRI",
  unicode: "U+1F1E8,U+1F1F7",
  nameEnglish: "Costa Rica"
};
