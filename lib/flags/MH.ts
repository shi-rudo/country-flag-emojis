import { CountryFlag } from "../types";

/**
 * Represents the country flag for Marshall Islands.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Marshall Islands flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Marshall Islands.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Marshall Islands.
 * @property {string} unicode - The Unicode code points for the Marshall Islands flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const MH: CountryFlag = {
  flag: "🇲🇭",
  isoAlpha2: "MH",
  isoAlpha3: "MHL",
  unicode: "U+1F1F2,U+1F1ED",
  nameEnglish: "Marshall Islands"
};
