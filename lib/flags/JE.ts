import { CountryFlag } from "../types";

/**
 * Represents the country flag for Jersey.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Jersey flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Jersey.
 * @property {string} unicode - The Unicode code points for the Jersey flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const JE: CountryFlag = {
  flag: "🇯🇪",
  isoAlpha2: "JE",
  unicode: "U+1F1EF,U+1F1EA",
  nameEnglish: "Jersey",
};

export default JE;
