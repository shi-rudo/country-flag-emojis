import { CountryFlag } from "../types";

/**
 * Represents the country flag for Canada.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Canada flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Canada.
 * @property {string} unicode - The Unicode code points for the Canada flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const CA: CountryFlag = {
  flag: "🇨🇦",
  isoAlpha2: "CA",
  unicode: "U+1F1E8,U+1F1E6",
  nameEnglish: "Canada",
};

export default CA;
