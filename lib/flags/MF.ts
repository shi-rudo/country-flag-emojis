import { CountryFlag } from "../types";

/**
 * Represents the country flag for Saint Martin (French Part).
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Saint Martin (French Part) flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Saint Martin (French Part).
 * @property {string} unicode - The Unicode code points for the Saint Martin (French Part) flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const MF: CountryFlag = {
  flag: "🇲🇫",
  isoAlpha2: "MF",
  unicode: "U+1F1F2,U+1F1EB",
  nameEnglish: "Saint Martin (French Part)",
};

export default MF;
