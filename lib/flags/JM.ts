import { CountryFlag } from "../types";

/**
 * Represents the country flag for Jamaica.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Jamaica flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Jamaica.
 * @property {string} unicode - The Unicode code points for the Jamaica flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const JM: CountryFlag = {
  flag: "🇯🇲",
  isoAlpha2: "JM",
  unicode: "U+1F1EF,U+1F1F2",
  nameEnglish: "Jamaica",
};

export default JM;
