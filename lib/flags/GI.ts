import { CountryFlag } from "../types";

/**
 * Represents the country flag for Gibraltar.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Gibraltar flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Gibraltar.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Gibraltar.
 * @property {string} unicode - The Unicode code points for the Gibraltar flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const GI: CountryFlag = {
  flag: "🇬🇮",
  isoAlpha2: "GI",
  isoAlpha3: "GIB",
  unicode: "U+1F1EC,U+1F1EE",
  nameEnglish: "Gibraltar"
};
