import { CountryFlag } from "../types";

/**
 * Represents the country flag for European Union.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the European Union flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for European Union.
 * @property {string} unicode - The Unicode code points for the European Union flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const EU: CountryFlag = {
  flag: "🇪🇺",
  isoAlpha2: "EU",
  unicode: "U+1F1EA,U+1F1FA",
  nameEnglish: "European Union",
};

export default EU;
