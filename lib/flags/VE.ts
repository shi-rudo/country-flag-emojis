import { CountryFlag } from "../types";

/**
 * Represents the country flag for Venezuela.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Venezuela flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Venezuela.
 * @property {string} unicode - The Unicode code points for the Venezuela flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const VE: CountryFlag = {
  flag: "🇻🇪",
  isoAlpha2: "VE",
  unicode: "U+1F1FB,U+1F1EA",
  nameEnglish: "Venezuela",
};

export default VE;
