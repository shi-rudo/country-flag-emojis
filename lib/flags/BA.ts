import { CountryFlag } from "../types";

/**
 * Represents the country flag for Bosnia and Herzegovina.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Bosnia and Herzegovina flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Bosnia and Herzegovina.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Bosnia and Herzegovina.
 * @property {string} unicode - The Unicode code points for the Bosnia and Herzegovina flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const BA: CountryFlag = {
  flag: "🇧🇦",
  isoAlpha2: "BA",
  isoAlpha3: "BIH",
  unicode: "U+1F1E7,U+1F1E6",
  nameEnglish: "Bosnia and Herzegovina"
};
