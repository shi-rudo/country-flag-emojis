import { CountryFlag } from "../types";

/**
 * Represents the country flag for Ireland.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Ireland flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Ireland.
 * @property {string} unicode - The Unicode code points for the Ireland flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const IE: CountryFlag = {
  flag: "🇮🇪",
  isoAlpha2: "IE",
  unicode: "U+1F1EE,U+1F1EA",
  nameEnglish: "Ireland",
};

export default IE;
