import { CountryFlag } from "../types";

/**
 * Represents the country flag for Denmark.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Denmark flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Denmark.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Denmark.
 * @property {string} unicode - The Unicode code points for the Denmark flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const DK: CountryFlag = {
  flag: "🇩🇰",
  isoAlpha2: "DK",
  isoAlpha3: "DNK",
  unicode: "U+1F1E9,U+1F1F0",
  nameEnglish: "Denmark"
};
