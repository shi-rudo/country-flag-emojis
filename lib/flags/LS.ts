import { CountryFlag } from "../types";

/**
 * Represents the country flag for Lesotho.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Lesotho flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Lesotho.
 * @property {string} unicode - The Unicode code points for the Lesotho flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const LS: CountryFlag = {
  flag: "🇱🇸",
  isoAlpha2: "LS",
  unicode: "U+1F1F1,U+1F1F8",
  nameEnglish: "Lesotho",
};

export default LS;
