import { CountryFlag } from "../types";

/**
 * Represents the country flag for Kenya.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Kenya flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Kenya.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Kenya.
 * @property {string} unicode - The Unicode code points for the Kenya flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const KE: CountryFlag = {
  flag: "🇰🇪",
  isoAlpha2: "KE",
  isoAlpha3: "KEN",
  unicode: "U+1F1F0,U+1F1EA",
  nameEnglish: "Kenya"
};
