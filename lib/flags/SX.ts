import { CountryFlag } from "../types";

/**
 * Represents the country flag for Sint Maarten (Dutch Part).
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Sint Maarten (Dutch Part) flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Sint Maarten (Dutch Part).
 * @property {string} unicode - The Unicode code points for the Sint Maarten (Dutch Part) flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const SX: CountryFlag = {
  flag: "🇸🇽",
  isoAlpha2: "SX",
  unicode: "U+1F1F8,U+1F1FD",
  nameEnglish: "Sint Maarten (Dutch Part)",
};

export default SX;
