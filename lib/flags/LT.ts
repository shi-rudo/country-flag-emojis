import { CountryFlag } from "../types";

/**
 * Represents the country flag for Lithuania.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Lithuania flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Lithuania.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Lithuania.
 * @property {string} unicode - The Unicode code points for the Lithuania flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const LT: CountryFlag = {
  flag: "🇱🇹",
  isoAlpha2: "LT",
  isoAlpha3: "LTU",
  unicode: "U+1F1F1,U+1F1F9",
  nameEnglish: "Lithuania"
};
