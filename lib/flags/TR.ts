import { CountryFlag } from "../types";

/**
 * Represents the country flag for Turkey.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Turkey flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Turkey.
 * @property {string} unicode - The Unicode code points for the Turkey flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const TR: CountryFlag = {
  flag: "🇹🇷",
  isoAlpha2: "TR",
  unicode: "U+1F1F9,U+1F1F7",
  nameEnglish: "Turkey",
};

export default TR;
