import { CountryFlag } from "../types";

/**
 * Represents the country flag for Finland.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Finland flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Finland.
 * @property {string} unicode - The Unicode code points for the Finland flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const FI: CountryFlag = {
  flag: "🇫🇮",
  isoAlpha2: "FI",
  unicode: "U+1F1EB,U+1F1EE",
  nameEnglish: "Finland",
};

export default FI;
