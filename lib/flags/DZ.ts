import { CountryFlag } from "../types";

/**
 * Represents the country flag for Algeria.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Algeria flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Algeria.
 * @property {string} unicode - The Unicode code points for the Algeria flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const DZ: CountryFlag = {
  flag: "🇩🇿",
  isoAlpha2: "DZ",
  unicode: "U+1F1E9,U+1F1FF",
  nameEnglish: "Algeria",
};

export default DZ;
