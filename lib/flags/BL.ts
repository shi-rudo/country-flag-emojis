import { CountryFlag } from "../types";

/**
 * Represents the country flag for Saint Barthélemy.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Saint Barthélemy flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Saint Barthélemy.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Saint Barthélemy.
 * @property {string} unicode - The Unicode code points for the Saint Barthélemy flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const BL: CountryFlag = {
  flag: "🇧🇱",
  isoAlpha2: "BL",
  isoAlpha3: "BLM",
  unicode: "U+1F1E7,U+1F1F1",
  nameEnglish: "Saint Barthélemy"
};
