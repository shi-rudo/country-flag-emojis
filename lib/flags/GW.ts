import { CountryFlag } from "../types";

/**
 * Represents the country flag for Guinea-Bissau.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Guinea-Bissau flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Guinea-Bissau.
 * @property {string} unicode - The Unicode code points for the Guinea-Bissau flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const GW: CountryFlag = {
  flag: "🇬🇼",
  isoAlpha2: "GW",
  unicode: "U+1F1EC,U+1F1FC",
  nameEnglish: "Guinea-Bissau",
};

export default GW;
