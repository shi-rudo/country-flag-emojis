import { CountryFlag } from "../types";

/**
 * Represents the country flag for Montenegro.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Montenegro flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Montenegro.
 * @property {string} unicode - The Unicode code points for the Montenegro flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const ME: CountryFlag = {
  flag: "🇲🇪",
  isoAlpha2: "ME",
  unicode: "U+1F1F2,U+1F1EA",
  nameEnglish: "Montenegro",
};

export default ME;
