import { CountryFlag } from "../types";

/**
 * Represents the country flag for Sweden.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Sweden flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Sweden.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Sweden.
 * @property {string} unicode - The Unicode code points for the Sweden flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const SE: CountryFlag = {
  flag: "🇸🇪",
  isoAlpha2: "SE",
  isoAlpha3: "SWE",
  unicode: "U+1F1F8,U+1F1EA",
  nameEnglish: "Sweden"
};
