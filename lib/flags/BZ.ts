import { CountryFlag } from "../types";

/**
 * Represents the country flag for Belize.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Belize flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Belize.
 * @property {string} unicode - The Unicode code points for the Belize flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const BZ: CountryFlag = {
  flag: "🇧🇿",
  isoAlpha2: "BZ",
  unicode: "U+1F1E7,U+1F1FF",
  nameEnglish: "Belize",
};

export default BZ;
