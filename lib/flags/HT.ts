import { CountryFlag } from "../types";

/**
 * Represents the country flag for Haiti.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Haiti flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Haiti.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Haiti.
 * @property {string} unicode - The Unicode code points for the Haiti flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const HT: CountryFlag = {
  flag: "🇭🇹",
  isoAlpha2: "HT",
  isoAlpha3: "HTI",
  unicode: "U+1F1ED,U+1F1F9",
  nameEnglish: "Haiti"
};
