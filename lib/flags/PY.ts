import { CountryFlag } from "../types";

/**
 * Represents the country flag for Paraguay.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Paraguay flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Paraguay.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Paraguay.
 * @property {string} unicode - The Unicode code points for the Paraguay flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const PY: CountryFlag = {
  flag: "🇵🇾",
  isoAlpha2: "PY",
  isoAlpha3: "PRY",
  unicode: "U+1F1F5,U+1F1FE",
  nameEnglish: "Paraguay"
};
