import { CountryFlag } from "../types";

/**
 * Represents the country flag for Belarus.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Belarus flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Belarus.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Belarus.
 * @property {string} unicode - The Unicode code points for the Belarus flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const BY: CountryFlag = {
  flag: "🇧🇾",
  isoAlpha2: "BY",
  isoAlpha3: "BLR",
  unicode: "U+1F1E7,U+1F1FE",
  nameEnglish: "Belarus"
};
