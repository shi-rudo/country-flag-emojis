import { CountryFlag } from "../types";

/**
 * Represents the country flag for Latvia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Latvia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Latvia.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Latvia.
 * @property {string} unicode - The Unicode code points for the Latvia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const LV: CountryFlag = {
  flag: "🇱🇻",
  isoAlpha2: "LV",
  isoAlpha3: "LVA",
  unicode: "U+1F1F1,U+1F1FB",
  nameEnglish: "Latvia"
};
