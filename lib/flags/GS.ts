import { CountryFlag } from "../types";

/**
 * Represents the country flag for South Georgia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the South Georgia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for South Georgia.
 * @property {string} unicode - The Unicode code points for the South Georgia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const GS: CountryFlag = {
  flag: "🇬🇸",
  isoAlpha2: "GS",
  unicode: "U+1F1EC,U+1F1F8",
  nameEnglish: "South Georgia",
};

export default GS;
