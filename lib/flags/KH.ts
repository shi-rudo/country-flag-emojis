import { CountryFlag } from "../types";

/**
 * Represents the country flag for Cambodia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Cambodia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Cambodia.
 * @property {string} unicode - The Unicode code points for the Cambodia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const KH: CountryFlag = {
  flag: "🇰🇭",
  isoAlpha2: "KH",
  unicode: "U+1F1F0,U+1F1ED",
  nameEnglish: "Cambodia",
};

export default KH;
