import { CountryFlag } from "../types";

/**
 * Represents the country flag for Mali.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Mali flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Mali.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Mali.
 * @property {string} unicode - The Unicode code points for the Mali flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const ML: CountryFlag = {
  flag: "🇲🇱",
  isoAlpha2: "ML",
  isoAlpha3: "MLI",
  unicode: "U+1F1F2,U+1F1F1",
  nameEnglish: "Mali"
};
