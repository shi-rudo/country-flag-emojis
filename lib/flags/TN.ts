import { CountryFlag } from "../types";

/**
 * Represents the country flag for Tunisia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Tunisia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Tunisia.
 * @property {string} isoAlpha3 - The ISO 3166-1 alpha-3 code for Tunisia.
 * @property {string} unicode - The Unicode code points for the Tunisia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
export const TN: CountryFlag = {
  flag: "🇹🇳",
  isoAlpha2: "TN",
  isoAlpha3: "TUN",
  unicode: "U+1F1F9,U+1F1F3",
  nameEnglish: "Tunisia"
};
