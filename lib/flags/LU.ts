import { CountryFlag } from "../types";

/**
 * Represents the country flag for Luxembourg.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Luxembourg flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Luxembourg.
 * @property {string} unicode - The Unicode code points for the Luxembourg flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const LU: CountryFlag = {
  flag: "🇱🇺",
  isoAlpha2: "LU",
  unicode: "U+1F1F1,U+1F1FA",
  nameEnglish: "Luxembourg",
};

export default LU;
