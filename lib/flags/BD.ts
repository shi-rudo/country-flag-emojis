import { CountryFlag } from "../types";

/**
 * Represents the country flag for Bangladesh.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Bangladesh flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Bangladesh.
 * @property {string} unicode - The Unicode code points for the Bangladesh flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const BD: CountryFlag = {
  flag: "🇧🇩",
  isoAlpha2: "BD",
  unicode: "U+1F1E7,U+1F1E9",
  nameEnglish: "Bangladesh",
};

export default BD;
