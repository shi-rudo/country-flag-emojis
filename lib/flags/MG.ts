import { CountryFlag } from "../types";

/**
 * Represents the country flag for Madagascar.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Madagascar flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Madagascar.
 * @property {string} unicode - The Unicode code points for the Madagascar flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const MG: CountryFlag = {
  flag: "🇲🇬",
  isoAlpha2: "MG",
  unicode: "U+1F1F2,U+1F1EC",
  nameEnglish: "Madagascar",
};

export default MG;
