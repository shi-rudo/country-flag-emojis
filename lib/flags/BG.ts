import { CountryFlag } from "../types";

/**
 * Represents the country flag for Bulgaria.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Bulgaria flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Bulgaria.
 * @property {string} unicode - The Unicode code points for the Bulgaria flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const BG: CountryFlag = {
  flag: "🇧🇬",
  isoAlpha2: "BG",
  unicode: "U+1F1E7,U+1F1EC",
  nameEnglish: "Bulgaria",
};

export default BG;
