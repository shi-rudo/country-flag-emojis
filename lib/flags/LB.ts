import { CountryFlag } from "../types";

/**
 * Represents the country flag for Lebanon.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Lebanon flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Lebanon.
 * @property {string} unicode - The Unicode code points for the Lebanon flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const LB: CountryFlag = {
  flag: "🇱🇧",
  isoAlpha2: "LB",
  unicode: "U+1F1F1,U+1F1E7",
  nameEnglish: "Lebanon",
};

export default LB;
