import { CountryFlag } from "../types";

/**
 * Represents the country flag for Nepal.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Nepal flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Nepal.
 * @property {string} unicode - The Unicode code points for the Nepal flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const NP: CountryFlag = {
  flag: "🇳🇵",
  isoAlpha2: "NP",
  unicode: "U+1F1F3,U+1F1F5",
  nameEnglish: "Nepal",
};

export default NP;
