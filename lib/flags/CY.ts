import { CountryFlag } from "../types";

/**
 * Represents the country flag for Cyprus.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Cyprus flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Cyprus.
 * @property {string} unicode - The Unicode code points for the Cyprus flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const CY: CountryFlag = {
  flag: "🇨🇾",
  isoAlpha2: "CY",
  unicode: "U+1F1E8,U+1F1FE",
  nameEnglish: "Cyprus",
};

export default CY;
