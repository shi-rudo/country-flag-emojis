import { CountryFlag } from "../types";

/**
 * Represents the country flag for Montserrat.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Montserrat flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Montserrat.
 * @property {string} unicode - The Unicode code points for the Montserrat flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const MS: CountryFlag = {
  flag: "🇲🇸",
  isoAlpha2: "MS",
  unicode: "U+1F1F2,U+1F1F8",
  nameEnglish: "Montserrat",
};

export default MS;
