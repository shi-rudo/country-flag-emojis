import { CountryFlag } from "../types";

/**
 * Represents the country flag for France.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the France flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for France.
 * @property {string} unicode - The Unicode code points for the France flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const FR: CountryFlag = {
  flag: "🇫🇷",
  isoAlpha2: "FR",
  unicode: "U+1F1EB,U+1F1F7",
  nameEnglish: "France",
};

export default FR;
