import { CountryFlag } from "../types";

/**
 * Represents the country flag for Slovakia.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Slovakia flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Slovakia.
 * @property {string} unicode - The Unicode code points for the Slovakia flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const SK: CountryFlag = {
  flag: "🇸🇰",
  isoAlpha2: "SK",
  unicode: "U+1F1F8,U+1F1F0",
  nameEnglish: "Slovakia",
};

export default SK;
