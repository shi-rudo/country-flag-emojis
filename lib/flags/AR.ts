import { CountryFlag } from "../types";

/**
 * Represents the country flag for Argentina.
 * 
 * @constant
 * @type {CountryFlag}
 * @property {string} flag - The emoji representation of the Argentina flag.
 * @property {string} isoAlpha2 - The ISO 3166-1 alpha-2 code for Argentina.
 * @property {string} unicode - The Unicode code points for the Argentina flag emoji.
 * @property {string} nameEnglish - The English name of the country.
 */
const AR: CountryFlag = {
  flag: "🇦🇷",
  isoAlpha2: "AR",
  unicode: "U+1F1E6,U+1F1F7",
  nameEnglish: "Argentina",
};

export default AR;
